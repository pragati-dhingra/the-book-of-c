#include <stdio.h>

    int main(int argc, char * argv[])
    {
    	double celsius=42.2, fahrenheit=0;
    	fahrenheit=((celsius * 9)/5) + 32;
    	printf(" The value of %f celsius in fahrenheit is %f \n", celsius, fahrenheit);
    	return 0;
	}
