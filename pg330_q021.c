#include <stdio.h>

int main (int argc, char* argv[] ){
    /* Write a program that:
    * Reads an int as input, on a line by itself
    * And prints "yes" if input number was a perfect square,
    * And prints "no" if input number was not a perfect square
     * Use bitwise operators. Assume all input is positive ints
     * Output is to be printed without quotes and without trailing newline  */
    return 0;
}
